import React from "react";
import Box from "components/atoms/Box/Box";
import "./Book.css";

export const Book = ({ chapter, pages }) => {
  return <Box className="horizontal-flex"></Box>;
};

export default Book;
